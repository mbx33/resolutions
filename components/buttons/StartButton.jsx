import React from 'react';

import { Button } from '../../styles/styled_components/utils';

const StartButton = () => {
	return (
		<Button cta primary>
			Start Now
		</Button>
	);
};

export default StartButton;
